# Go Version (compile locally)

The Go version uses [bubbletea](https://github.com/charmbracelet/bubbletea) for a richer TUI.

## Build

```bash
cd go-src
go build -o crypto-terminal .
./crypto-terminal
```

Requires Go 1.21+.
